<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="demo.xm.com.demo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.custom.CustomViewActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".activity.life.A_activity" />
        <activity android:name=".activity.life.B_activity" />
        <activity
            android:name=".activity.life.ChangeActivity"
            android:configChanges="orientation|keyboardHidden|screenSize" />
        <activity android:name=".activity.frg.ExampleActivity" />
        <!-- <activity android:name=".view.basic.BasicActivity"></activity> -->
        <!-- <activity android:name=".view.event.EventActivity"></activity> -->
        <!-- <activity android:name=".view.event.slidingconflict.SlidingActivity"></activity> -->
        <!--<activity android:name=".view.custom.CustomViewActivity"></activity>-->
    </application>

</manifest>